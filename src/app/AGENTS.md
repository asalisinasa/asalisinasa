# App

Next.js App Router entrypoint.

- Keep route pages and layouts here.
- Keep route-local sections and shell components under the route group that owns them.
- `not-found.tsx` is the 404 UI; `error.tsx` / `global-error.tsx` cover runtime 500 UI (shared shell in `_components/system-status`).
