# Application source

- `app/` owns routes, layouts, global CSS, and route-local sections.
- `components/` is for shared UI with more than one real consumer.
- `content/` is the local typed source of portfolio copy and lightweight config.
- `styles/` is for global tokens and shared CSS primitives.
- `lib/` is for shared non-UI logic with more than one real consumer.
