# Application source

- `app/` owns routes, layouts, global CSS, and route-local sections.
- `ui/` is for shared UI with more than one real consumer.
- `sanity/` — directory for integration with Sanity (headless CMS).
