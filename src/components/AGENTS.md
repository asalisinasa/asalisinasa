# Components

Shared UI components live here only when they have more than one real consumer.

- Keep primitives explicit and presentation-only.
- Use CSS Modules for component styles.
- Prefer route-local components until reuse is real.
